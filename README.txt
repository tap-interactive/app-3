To run the app on the box:
1, To the USB move: app.lua, lib and src
2, Change row 5 in app.lua, PATH variable, to the current USB path
3, Run file app.lua according to instructions in ../doc/lua_engine_usage.md
